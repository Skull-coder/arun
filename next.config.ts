import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  allowedDevOrigins: ['angelic-covenant-shriek.ngrok-free.dev', 'arun.skullcoder.dev'],
};

export default nextConfig;
